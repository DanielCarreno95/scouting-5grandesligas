[theme]
primaryColor = "#1f77b4"
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#1B1F24"
textColor = "#FAFAFA"

[server]
fileWatcherType = "none"   # <- desactiva el watcher que abre muchos ficheros
# Opcional:
# runOnSave = false         # evita relanzar la app al guardar (si editas en servidor)
# maxUploadSize = 200       # si quisieras subir CSVs grandes en Shortlist
